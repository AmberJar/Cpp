#include <iostream>
#include <string>
#include <vector>
using namespace std;

void sum_adjacent(const vector<int> &iv)
{
    if (iv.size() < 2)
        return;
    
    for (decltype(iv.size()) i = 0; i < v.size(); )

}



int main()
{
    vector<int> a;
    int i;

    while(cin >> i)
        a.push_back(i);

    sum_adjacent(a);

    return 0;
}